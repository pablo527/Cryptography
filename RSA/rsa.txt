

docker build -t rjmp1991/rsaoaep:amd64 .
docker container run -d --name rsa --rm rjmp1991/rsaoaep:arm64
docker container inspect --format '{{ .NetworkSettings.IPAddress }}' rsa

#Validar en el navegador
http://172.17.0.4/encrypt/

#subir al registry
docker push rjmp1991/rsaoaep:amd64

#correr en el cluster
kubectl apply -f encrypt.yaml
kubectl get all
kubectl apply -f expose.yaml

#Validar en el navegador
https://ingress.gistsandbox.com:{puerto}/encrypt/
